# archive/

Superseded docs and old data. Nothing here describes the current system —
kept for historical context only.

- `PLAN.md` — original architecture plan (pre-evolution-spec). Superseded by
  `../evolution/`; see `../README.md` for what of it was absorbed vs dropped.
- `01_current_state.md` — pre-WP1 as-is/defect analysis. The defects are fixed.
- `graph-exports/` — Neo4j JSON snapshots from the three-graph comparison
  (Graph 1 aikg :7688, Graph 2 pipeline :7687, Graph 3 report :7689).
